import React from 'react';
import './css/spinner/spinning-pixels.css';

const SpinningPixels = props => {
  return(
    <div className="spinning-pixels-loader" {...props} />
  )
}

export { SpinningPixels };
